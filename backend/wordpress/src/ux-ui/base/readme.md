# Base directory from "The 7-in-1 SCSS Architecture"

This is the boilerplate styles used globally across the application. In this directory we would include our default CSS resets and typography styles.
